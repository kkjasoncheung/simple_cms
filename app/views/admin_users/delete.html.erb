<% @page_title = "Delete Admin User" %>

<%= error_messages_for(@admin_user) %>

<%= render(:partial=>'shared/back') %>

<div class="admin_users delete">

	<h2>Delete Admin User</h2>
	<%= form_for(:admin_user, :url=>{:action=>'destroy', :id=>@admin_user.id}) do |f| %>

	<p>Are you sure you want to permanently delete <strong><%= @admin_user.name %> </strong> ? </p>

		<div class="form-buttons">
				
			<%= submit_tag('Delete') %>

		</div>


	<% end %>
</div>